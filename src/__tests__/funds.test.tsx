import * as React from "react";
import renderer from 'react-test-renderer';


test('Funds render as a table of names and balances', () => {
    const component = renderer.create(
        <div>
        </div>
    );
});